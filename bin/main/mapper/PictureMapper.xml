<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.workat.workat_project.picture.repository.PictureMapper">

    <insert id="insertUserReviewPicture">
        insert into review_picture
        (picture_source,
         picture_origin_name,
         review_id)
        values
            (#{fileSource},#{fileName},#{reviewId})
    </insert>

    <update id="updateUserReviewSource" parameterType="map">
        UPDATE review_picture
            SET status = 'inactive'
            WHERE review_id = #{reviewId}
            AND picture_source NOT IN
            <foreach item="item" collection="src" open="(" separator="," close=")">
                #{item}
            </foreach>
    </update>
    <update id="updateUserReviewSourceOne">
        UPDATE review_picture
            SET status = 'inactive'
            WHERE review_id = #{reviewId}
    </update>

    <delete id="deleteReviewPicture">
        update
        review_picture
        set
            status = "inactive"
        where
            review_id = #{review_id}
    </delete>

    <select id="getPlacePictureSources" resultType="String">
        SELECT picture_source
        FROM place_picture
        WHERE place_id = #{placeId}
        AND status = "active"
    </select>

    <select id="getRoomPictureSources" resultType="String">
        SELECT picture_source
        FROM room_picture
        WHERE room_id = #{roomId}
        AND status = "active"

    </select>

    <select id="getReviewPictureSources" resultType="String">
        SELECT picture_source
        FROM review_picture
        WHERE review_id = #{reviewId}
      AND status = "active"

    </select>

</mapper>